# Stages 00-02 (roster, pageviews, ranking) need no API keys.
# Later stages will need:
# CO_API_KEY=        # Cohere, for embeddings (stage 05)
# ANTHROPIC_API_KEY= # Anthropic, for Toponymy region naming (stage 07)
